# ─── Email Configuration (Brevo SMTP) ─────────────────
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=a289a8001@smtp-brevo.com
spring.mail.password=${BREVO_SMTP_KEY:your-brevo-smtp-key}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# ─── JWT Configuration ──────────────────────────────
jwt.secret=${JWT_SECRET:PLACEHOLDER_JWT_SECRET_MUST_BE_CHANGED}
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# ─── Firebase Configuration ─────────────────────────
# (If you still use Firebase Admin SDK for other things)
# firebase.config.path=classpath:firebase-service-account.json
