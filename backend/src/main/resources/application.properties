# ─── Email Configuration (Brevo API) ──────────────────
# This is more reliable than SMTP as it uses Port 443 (never blocked by Render)
brevo.api.key=${BREVO_API_KEY:your-brevo-api-key}
spring.mail.from=physicsparadox369@gmail.com

# ─── JWT Configuration ──────────────────────────────
jwt.secret=${JWT_SECRET:PLACEHOLDER_JWT_SECRET_MUST_BE_CHANGED}
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# ─── Firebase Configuration ─────────────────────────
# (If you still use Firebase Admin SDK for other things)
# firebase.config.path=classpath:firebase-service-account.json
